{
  "name": "lifelink-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "description": "LifeLink — Real-Time Healthcare Inventory Sharing Platform",
  "scripts": {
    "dev": "corepack pnpm --parallel --filter @lifelink/* dev",
    "dev:api": "corepack pnpm --filter @lifelink/api dev",
    "dev:web": "corepack pnpm --filter @lifelink/web dev",
    "build": "corepack pnpm --filter @lifelink/shared build && corepack pnpm --filter @lifelink/api build && corepack pnpm --filter @lifelink/web build",
    "test": "corepack pnpm --filter @lifelink/api test",
    "db:generate": "corepack pnpm --filter @lifelink/api db:generate",
    "db:push": "corepack pnpm --filter @lifelink/api db:push",
    "db:migrate": "corepack pnpm --filter @lifelink/api db:migrate",
    "db:seed": "corepack pnpm --filter @lifelink/api db:seed",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  }
}
